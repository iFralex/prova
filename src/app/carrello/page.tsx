import { getCartsFromCartsLight, getCookie } from "@/actions/get-data"
import { getCartsLight, pushCartData } from "@/actions/firebase"
import { CartClient } from "./client"

const Page = async () => {
   
    return <CartClient><></></CartClient>
}

export default Page